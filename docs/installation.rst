============
Installation
============

At the command line::

    $ easy_install mommy_spatial_generators

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mommy_spatial_generators
    $ pip install mommy_spatial_generators
